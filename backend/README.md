# backend/

Placeholder folder. Nothing here is prescribed — rename it, split it into
`api/` + `collector/`, merge it into a single top-level `src/`, whatever fits
your architecture. Delete this file once your own code lives here.

Suggested (not required) preferred stack per the task brief: Falcon, pylxd,
sqlite3, TinyFlux. Put your `requirements.txt` / `pyproject.toml` here (or
wherever your structure ends up).
